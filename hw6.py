def area_triangle(h, w):
    return 0.5 * h * w


def distance(a,b):
    a = (x,y)
    b = (m,n)
    for 


    
    
    
    
def count(n):
    if n > 0 :
        print(n)
        return count(n-1)
    else :
        return 'zero!!'

    
    

area_triangle_ld = lambda h, w: 0.5 * h * w
